// Styles
import "./button.css";

function Button({ buttonText }) {
    return (
        <button className="link-header-button">
            {buttonText}
        </button>
    );
}

export default Button;